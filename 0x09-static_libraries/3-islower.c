#include "main.h"

/**
 * _islower -  used to check whether a character is a lowercase letter or not.
 * @c: the character to check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
